<function-spec name="-">
  <syntax>(- number ...)</syntax>

  <description><p>When called with one argument, returns the additive
  inverse (zero minus the number). When called with more than one
  argument, subtracts each of the arguments after the first argument
  from the first argument and returns the result.</p></description>

  <parameters>
    <parameter name="number ...">One or more numbers</parameter>
  </parameters>
  <examples>
    <example expr="(- 0)" value="0"/>
    <example expr="(- 2)" value="-2"/>
    <example expr="(- -3)" value="3"/>
    <example expr="(- 5 2)" value="3"/>
    <example expr="(- 5 2 7)" value="-4"/>
  </examples>
</function-spec>
